<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Jogador</title>
    <link rel="stylesheet" href="/CSS/style.css">
  </head>
  <body>

    <div class="menu">
      <h3>
        <a href="/">Início</a>
        <a href="/list/games">Listagem de Jogos</a>
        <a href="/rank">Ranking</a>
      </h3>
    </div>


    <div class="data">

    <h1 class="player">{{name}}</h1>
      <div class="analyzes">
      <h3>Dados do Jogador</h3>
        <p>Total de Jogos:
          {{games.length}}</p>
        <p>Total de Vitorias:
          {{totvictory}}</p>
        <p>Total de Derrotas:
          {{totdefeat}}</p>
        <p>Total de Empates:
          {{tottie}}</p>
        <p>Média de Gols por Partida:
          {{totaveragegoalsmatch}}</p>
        <p>Quantas Vezes Ganho Marcando +0,5 Gol:
          {{totonegoalsposit}}</p>
        <p>Quantas Vezes Ganho Marcando +1,5 Gol:
          {{tottwogoalsposit}}</p>
        <p>Quantas Vezes Ganho Marcando +2,5 Gol:
          {{totthreegoalsposit}}</p>
        <p>Quantas Vezes Perdeu Recebendo -0,5 Gol:
          {{totonegoalsneg}}</p>
        <p>Quantas Vezes Perdeu Recebendo -1,5 Gol:
          {{tottwogoalsneg}}</p>
        <p>Quantas Vezes Perdeu Recebendo -2,5 Gol:
          {{totthreegoalsneg}}</p>
      </div>

    </div>

    <div>

      <table class="table show">

        <thead>

          <tr class="head show">

            <th>Dia</th>
            <th>Hora</th>
            <th>Primeiro Jogador</th>
            <th>Segundo Jogador</th>
            <th>Placar</th>
            <th>Resultado</th>

          </tr>

        </thead>

        {% for game in games %}
          <tbody>

            <tr class="body show">
              <td>{{game.day}}</td>
              <td>{{game.hour}}</td>
              <td>
                <a href="/show/{{game.play1}}">{{game.play1}}</a>
              </td>
              <td>
                <a href="/show/{{game.play2}}">{{game.play2}}</a>
              </td>
              <td>{{game.rslplay1}} X {{game.rslplay2}}</td>
              {% if ((game.victoryplay1 == true) and ((game.play1 == name) or (game.play1name == name))) %}
                <td class="win">Venceu</td>
              {% elif((game.victoryplay2 == true) and ((game.play2 == name) or (game.play2name == name))) %}
                <td class="win">Venceu</td>
              {% elif(game.tied == true) %}
                <td class="tie">Empate</td>
              {% else %}
                <td class="defeat">Perdeu</td>
              {% endif %}
            </tr>

          </tbody>
        {% endfor %}

      </table>

    </div>
  </body>
</html>